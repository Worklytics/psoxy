fileRules:
  # events
  /{exportId}/events.ndjson:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.actor.id"
          - "$.actor.identity.id"
          - "$.object.collaborators[*].id"
          - "$.object.collaborators[*].identity.id"
  /{exportId}/events{shardIndex}.ndjson:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.actor.id"
          - "$.actor.identity.id"
          - "$.object.collaborators[*].id"
          - "$.object.collaborators[*].identity.id"
  /{exportId}/events.ndjson.gz:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.actor.id"
          - "$.actor.identity.id"
          - "$.object.collaborators[*].id"
          - "$.object.collaborators[*].identity.id"
  /{exportId}/events{shardIndex}.ndjson.gz:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.actor.id"
          - "$.actor.identity.id"
          - "$.object.collaborators[*].id"
          - "$.object.collaborators[*].identity.id"

  # items (snapshots)
  /{exportId}/items.ndjson:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.collaborators[*].id"
          - "$.collaborators[*].identity.id"
          - "$.object.collaborators[*].id"
          - "$.object.collaborators[*].identity.id"
  /{exportId}/items{shardIndex}.ndjson:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.collaborators[*].id"
          - "$.collaborators[*].identity.id"
          - "$.object.collaborators[*].id"
          - "$.object.collaborators[*].identity.id"
  /{exportId}/items.ndjson.gz:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.collaborators[*].id"
          - "$.collaborators[*].identity.id"
          - "$.object.collaborators[*].id"
          - "$.object.collaborators[*].identity.id"
  /{exportId}/items{shardIndex}.ndjson.gz:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.collaborators[*].id"
          - "$.collaborators[*].identity.id"
          - "$.object.collaborators[*].id"
          - "$.object.collaborators[*].identity.id"

  # accounts
  /{exportId}/accounts.ndjson:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.accountId"
          - "$.associatedIdentities[*].id"
  /{exportId}/accounts{shardIndex}.ndjson:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.accountId"
          - "$.associatedIdentities[*].id"
  /{exportId}/accounts.ndjson.gz:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.accountId"
          - "$.associatedIdentities[*].id"
  /{exportId}/accounts{shardIndex}.ndjson.gz:
    format: "NDJSON"
    transforms:
      - pseudonymize:
          - "$.accountId"
          - "$.associatedIdentities[*].id"
