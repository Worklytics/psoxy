allowedEndpointRegexes:
  - "^/(v1.0|beta)/(groups|users)/?[^/]*"
  - "^/(v1.0|beta)/(groups|users)\\?.*"
  - "^/(v1.0|beta)/groups/[^/]*/members[^/]*"
pseudonymizations:
  - jsonPaths:
      - "$..userPrincipalName"
      - "$..imAddresses[*]"
      - "$..mail"
      - "$..otherMails[*]"
    relativeUrlRegex: "^/(v1.0|beta)/users.*"
  - jsonPaths:
      - "$..userPrincipalName"
      - "$..imAddresses[*]"
      - "$..mail"
      - "$..otherMails[*]"
    relativeUrlRegex: "^/(v1.0|beta)/groups/[^/]*/members.*"
redactions:
  - jsonPaths:
      - "$..displayName"
      - "$..employeeId"
      - "$..aboutMe"
      - "$..mySite"
      - "$..preferredName"
      - "$..givenName"
      - "$..surname"
      - "$..mailNickname"
      - "$..proxyAddresses"
      - "$..faxNumber"
      - "$..mobilePhone"
      - "$..businessPhones[*]"
    relativeUrlRegex: "^/(v1.0|beta)/users.*"
  - jsonPaths:
      - "$..owners"
      - "$..rejectedSenders"
      - "$..acceptedSenders"
      - "$..members"
      - "$..membersWithLicenseErrors"
    relativeUrlRegex: "^/(v1.0|beta)/groups.*"
  - jsonPaths:
      - "$..displayName"
      - "$..employeeId"
      - "$..aboutMe"
      - "$..mySite"
      - "$..preferredName"
      - "$..givenName"
      - "$..surname"
      - "$..mailNickname"
      - "$..proxyAddresses"
      - "$..faxNumber"
      - "$..mobilePhone"
      - "$..businessPhones[*]"
    relativeUrlRegex: "^/(v1.0|beta)/groups/[^/]*/members.*"
