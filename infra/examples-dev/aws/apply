#!/bin/bash
# Apply this examples-dev Terraform configuration after auth/backend preflight.

set -euo pipefail

EXAMPLE_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "${EXAMPLE_DIR}/../../.." && pwd)"

cd "$EXAMPLE_DIR"
exec "${REPO_ROOT}/tools/examples-dev-apply.sh"
